import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; import fs from 'fs'; dotenv.config(); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_SERVICE_ROLE_KEY || process.env.VITE_SUPABASE_ANON_KEY); async function main() { const { data, error } = await supabase.from('suggestions').select('*'); if (error) { console.error('Error fetching suggestions:', error); return; } fs.writeFileSync('c:\\Users\\marco\\OneDrive\\Desktop\\manualeciv\\scripts\\all_supabase_suggestions.json', JSON.stringify(data, null, 2)); console.log('Successfully saved ' + data.length + ' suggestions to all_supabase_suggestions.json'); } main();
